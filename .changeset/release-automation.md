---
'@ventostack/file-parser': patch
'@ventostack/vm-runtime': patch
---

Set up automated npm publishing: explicit cross-platform build targets, public access, and per-platform packages (`@ventostack/*-darwin-arm64` etc.).
